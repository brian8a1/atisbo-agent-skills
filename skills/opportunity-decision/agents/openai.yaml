interface:
  display_name: "Atisbo Opportunity Decision"
  short_description: "Decide what to do about an Opportunity"
  default_prompt: "Use $opportunity-decision to help me decide what to do about this Atisbo Opportunity."
dependencies:
  tools:
    - type: "mcp"
      value: "atisbo"
      description: "Atisbo Signals, Opportunities, Backlog, Decisions, and Outcomes"
      transport: "streamable_http"
      url: "https://app.atisbo.dev/api/mcp"
policy:
  allow_implicit_invocation: true
