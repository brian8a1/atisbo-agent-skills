interface:
  display_name: "Atisbo Product Review"
  short_description: "Prepare weekly and roadmap reviews"
  default_prompt: "Use $product-review to prepare my product review from what changed in Atisbo."
dependencies:
  tools:
    - type: "mcp"
      value: "atisbo"
      description: "Atisbo Signals, Opportunities, Backlog, Decisions, and Outcomes"
      transport: "streamable_http"
      url: "https://app.atisbo.dev/api/mcp"
policy:
  allow_implicit_invocation: true
