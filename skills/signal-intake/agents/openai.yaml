interface:
  display_name: "Atisbo Signal Intake"
  short_description: "Bring research and evidence into Signals"
  default_prompt: "Use $signal-intake to add this source to Atisbo with its provenance intact."
dependencies:
  tools:
    - type: "mcp"
      value: "senso"
      description: "Atisbo Signals, Opportunities, Backlog, Decisions, and Outcomes"
      transport: "streamable_http"
      url: "https://app.atisbo.dev/api/mcp"
policy:
  allow_implicit_invocation: true
