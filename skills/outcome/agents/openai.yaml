interface:
  display_name: "Atisbo Outcome"
  short_description: "Record post-launch impact and learning"
  default_prompt: "Use $outcome to review whether this Done Atisbo Solution produced the expected result."
dependencies:
  tools:
    - type: "mcp"
      value: "senso"
      description: "Atisbo Signals, Opportunities, Backlog, Decisions, and Outcomes"
      transport: "streamable_http"
      url: "https://app.atisbo.dev/api/mcp"
policy:
  allow_implicit_invocation: true
