interface:
  display_name: "Atisbo Backlog Brief"
  short_description: "Brief the work already in front of you"
  default_prompt: "Use $backlog-brief to brief me on this Atisbo Backlog item before I continue my work."
dependencies:
  tools:
    - type: "mcp"
      value: "atisbo"
      description: "Atisbo Signals, Opportunities, Backlog, Decisions, and Outcomes"
      transport: "streamable_http"
      url: "https://app.atisbo.dev/api/mcp"
policy:
  allow_implicit_invocation: true
